<script lang="ts">
  // Shown while playback runs in its own window. Grid minimizes, so this is on
  // screen only briefly before and after - but it is the only place the user is
  // told how to switch audio and subtitles, since those are the player's own
  // keys rather than Grid's controls.
</script>

<div class="border-primary/30 bg-surface/40 mb-6 rounded border p-4">
  <p class="text-main text-sm font-bold tracking-wider uppercase">
    Reproduzindo em uma janela separada
  </p>
  <p class="text-muted mt-2 text-sm">
    Use <kbd class="text-green font-bold">#</kbd> para trocar o áudio e
    <kbd class="text-green font-bold">j</kbd> para as legendas. Feche a janela do vídeo para voltar.
  </p>
</div>
